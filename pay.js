var array = ["May", "Ben", "Jenny", "Michael", "Chloe"];

function whosPaying(names) {
    var r = Math.random();
    r = Math.floor(r * names.length);
    return names[r]+" is going to buy lunch today!";
    return s;
}

console.log(whosPaying(array));
